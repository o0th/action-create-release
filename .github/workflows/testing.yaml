name: testing 

on:
  push:
    branches:
      - master 

permissions:
  contents: write

jobs:
  testing:
    runs-on: ubuntu-latest

    steps:
      - name: Set up source
        uses: actions/checkout@v4

      - name: Set up zig
        uses: goto-bus-stop/setup-zig@v2
        with:
          version: 0.13.0

      - name: acation-release
        uses: ./
