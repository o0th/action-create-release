name: action-release
description: create tags and release

branding:
  icon: cast
  color: red

inputs:

  token:
    required: false
    default: ${{ github.token }}

runs:
  using: 'node20'
  main: 'index.js'
